//string handling functions
#include<stdio.h>
#include<string.h>

int main()
{
    /* ===========  string length ========= */


    // char name[100];
    
    // printf("Enter your name : ");
    // fgets(name, 100, stdin);

    /* ==========================
            Type Casting
       ========================== */ 
       
       // strlen() returns unsigned long

    // int length = strlen(name);

    // printf("length is %d\n", length);

    // printf("Length = %lu\n", strlen(name)); 

    // strlen() returns unsigned long 


    /* =========== string copy =========== */

    // char oldStr[100];
    // char newStr[100];

    // printf("Enter first string : ");
    // fgets(oldStr, 100, stdin);

    // printf("Enter second string : ");
    // fgets(newStr, 100, stdin);

    // strcpy(oldStr, newStr);
    // puts(oldStr);

    // printf("Old string : %s", strcpy(oldStr, newStr));



    /* ========== string concatnation ========= */

    // char yourPass[100] = "santosh";
    // char salt[50] = "743@gmail.com";

    // printf("Your new password is : %s", strcat(yourPass, salt));

    // strcat(yourPass, salt);
    // puts(yourPass);




    /* ==========  string comparision =========== */

    // char firstStr[100];
    // char secStr[100];

    // printf("Enter first string : ");
    // fgets(firstStr, 100, stdin);

    // printf("Enter second string : ");
    // fgets(secStr, 100, stdin);

    // printf("%d\n", strcmp(firstStr, secStr));

    return 0;
}
